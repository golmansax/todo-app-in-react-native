var React = require('react-native');
var { Text } = React;

class TodosNewView extends React.Component {
  render() {
    return (
      <Text>
        Add!
      </Text>
    );
  }
}

module.exports = TodosNewView;
